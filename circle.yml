machine:
  python:
    version: 2.7.11

dependencies:
  override:
    - echo "dependencies"
test:
  override:
    - echo "test"

deployment:
  nightly:
    branch: master
    owner: scikit-build
    commands:
      - echo "deployment-nightly"
  release:
    tag: /v[0-9]+\.[0-9]+\.[0-9]+/
    owner: scikit-build
    commands:
      - echo "deployment-release"
